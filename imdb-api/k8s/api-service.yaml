apiVersion: v1
kind: Service
metadata:
  name: imdb-api-service
  namespace: imdb
spec:
  type: NodePort
  selector:
    app: imdb-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
